# SHORT Trading Environment - Example Configuration
# Copy to .env.development.short.v2 or .env.production.short.v2

# Trading Side (MUST be SHORT for this system)
TRADE_SIDE=SHORT

# Server Port
PORT=8789

# Binance API Credentials
BINANCE_API_KEY=your_binance_api_key_here
BINANCE_SECRET_KEY=your_binance_secret_here
BINANCE_RECV_WINDOW_MS=120000

# OpenAI API (for GPT-based features)
OPENAI_API_KEY=sk-your_openai_key_here
OPENAI_ORG_ID=org-your_org_id_here
OPENAI_PROJECT=proj_your_project_id_here

# Health Monitor Configuration
HEALTH_MONITOR_PROVIDER=server
# Options: 'server' (fast, deterministic) or 'gpt' (GPT-4o powered)

HEALTH_MONITOR_MODEL=gpt-4o-mini
# Used only if HEALTH_MONITOR_PROVIDER=gpt
# Options: 'gpt-4o-mini' (cheaper, faster) or 'gpt-4o' (more sophisticated)

# PM2 Process Name (optional)
PM2_NAME=trader-short-v2

# Node Environment
NODE_ENV=development
# Options: 'development' or 'production'

# Debug Mode
DEBUG_API=false
