{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://schemas.local/ai_profit_taker.schema.json",
  "title": "AIProfitTakerDecision",
  "version": 1,
  "description": "AI Profit Taker decision schema for SHORT positions - adjusts SL/TP orders based on market analysis",
  "type": "object",
  "required": ["action", "symbol", "new_sl", "new_tp", "rationale", "confidence"],
  "additionalProperties": false,
  "properties": {
    "action": {
      "type": "string",
      "enum": ["adjust_exits", "skip"],
      "description": "Action to take: adjust_exits to modify SL/TP, skip to keep current orders"
    },
    "symbol": {
      "type": "string",
      "minLength": 1,
      "description": "Trading symbol (e.g., BTCUSDT)"
    },
    "new_sl": {
      "type": ["number", "null"],
      "description": "New stop loss price (null = keep current). For SHORT: SL is ABOVE entry"
    },
    "new_tp": {
      "type": ["number", "null"],
      "description": "New take profit price (null = keep current). For SHORT: TP is BELOW entry"
    },
    "rationale": {
      "type": "string",
      "minLength": 1,
      "maxLength": 300,
      "description": "1-3 sentences explaining the decision"
    },
    "confidence": {
      "type": "number",
      "minimum": 0,
      "maximum": 1,
      "description": "Confidence level (0-1). High confidence (>0.7) = strong recommendation"
    }
  }
}

